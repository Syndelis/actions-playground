name: 'Dummy'
author: 'Brenno Lemos'
description: 'TODO'

inputs:
  github_token:
    description: 'GitHub Token'
    required: true

env:
  PASSING: true

runs:
  using: composite
  steps:

    - name: A
      id: a
      if: env.PASSING == 'true'
      shell: bash
      env:
        GITHUB_TOKEN: ${{ inputs.github_token }}
      run: |
        echo "PASSING=false" >> "$GITHUB_ENV"
    
    - name: B
      id: b
      if: env.PASSING == 'true'
      shell: bash
      env:
        GITHUB_TOKEN: ${{ inputs.github_token }}
      run: |
        echo "nine=9" >> "$GITHUB_OUTPUT"
    
    - name: Validate outputs
      shell: bash
      env:
        GITHUB_TOKEN: ${{ inputs.github_token }}
      run: |
        echo "A said : ${{ steps.a.outputs.eight }}"
        echo "B said : ${{ steps.b.outputs.nine }}"
